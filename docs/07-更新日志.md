# 07 - 更新日志（CHANGELOG）

> 记录每一次版本变更的**更新内容**。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。
> 每个版本的条目由开发者在发布时从开发日志汇总而来，按「新增 / 修复 / 变更 / 移除 / 文档」分类。

---

## 版本规则

- `x.y.z`：主版本（不兼容 API 变更）· 次版本（向后兼容的新功能）· 补丁（向后兼容的 bug 修复）
- 未发布内容记录在 **Unreleased** 段，发布时移动到具体版本号
- 每次用户可见的变更（功能、修复、配置项）都必须记入

---

## Unreleased

**修复与平台优化（2026-08-14 · CI 构建修复 + Docker 磁盘过滤 + 备份目录落盘）✅**
- **修复 CI 构建失败**（9e224e6）：根因是提交 3416da5 移除 `frontend/static` 回退后，`test_static_index_served` 依赖 `frontend/dist`，而 `dist` 被 gitignore、CI 从不构建前端 → `GET /` 404 测试失败、build job 被跳过。修复：test job 增加 `setup-node` + `npm ci && npm run build`（业界标准）；该测试在 dist 缺失时 `pytest.skip` 兜底（本地开发可不构建前端跑 pytest）
- **Docker 部署磁盘过滤**（6f7b0fc）：`platform.py` 新增 `_running_in_docker()`（`/.dockerenv` / `/run/.containerenv`）；`linux_disks()` 在容器内**只展示 compose bind 进来的挂载盘**（`/vault`、`/data`、额外挂载的 `/mnt/xxx`），排除根挂载与 `/mnt /media` 兜底目录——「切换笔记库」不再列出容器内部目录；**本地裸机部署保留全量枚举 + 兜底**（逻辑不变）
- **备份目录落挂载盘**：`docker-compose.yml` 新增 `BACKUP_DIR=/data/backups`——默认备份目录是 `{项目根}/backups`（Docker 内 `/app/backups`，容器可写层，**重建容器即丢**），显式配置到挂载盘后持久化
- **文档同步**：README 结构/路线图、04 配置参考（`BACKUP_DIR` 默认值修正为 `{项目根}/backups`、`BACKUP_SCHEDULE` 标注废弃）、03 快速开始、`.env.example` 补 `BACKUP_DIR` 与活跃式自动备份字段
- 新增测试：Docker 模式只展示挂载盘 / 非 Docker 保留兜底 2 例；全量 `pytest` 156 passed

**模块化优化 S25（2026-08-02 · 前端编排层瘦身 + 渲染/DOM 分离 + 清理）✅**
- **App.vue 内联逻辑下沉为独立 composables**（410→316 行，纯编排）：
  - `useHashRouter.js`：Hash 路由（视图/文档进 URL、前进后退、防循环）
  - `useVaultSwitcher.js`：库切换（路径/检测/切换 + 切换后视图重置回调）
  - `useCreateDoc.js`：新建笔记对话框 + 创建逻辑
- **md.js 渲染与 DOM 增强分离**：`mdEnhance.js`（callout/任务列表/搜索高亮 + escapeRegex），md.js 371→299 行只管 markdown 渲染
- **DocView TOC 逻辑抽 `useToc.js`**（270→217 行）：点击跳转 + 滚动高亮（IO + scroll 双机制）
- **清理垃圾文件** `frontend/src/_mem2.py`（临时脚本误留）
- 回归验证：文档打开/hash 路由/TOC 130 项点击高亮/新建对话框/备份管理 tab 全通过；构建通过

**优化（2026-08-01 · 备份管理 UI/删除/默认目录）**
- **默认备份目录改为项目根 `backups/`**：Docker 部署时宿主卷挂到 `/app/backups` 即可（`config.py resolved_backup_dir` 回退值从 `{data_dir}/backups` 改为 `{项目根}/backups`）
- **按钮文案**：「打开备份目录」→「📂 打开目录」、「更换备份目录」→「📝 修改目录」，卡片标题加 emoji
- **删除快照改异步**：Windows 硬链接删除慢（1770 文件约 1-2 分钟），同步会卡死前端「删除中」。改为 `DELETE /api/backup/{id}` 立即返回 202，后台线程执行 `rmtree_manual` 清理；前端标记删除中，3s 轮询直到快照从列表消失
- **自动备份间隔默认 30 分钟**（config 默认值），前端可修改（1~1440 钳制）并持久化 settings.json
- **自动备份卡片 UI 重做**：间隔输入 + 保存按钮 + 说明文字分行（活跃才备、空闲不备、关页即停）

### 全量模块化梳理（2026-08-01 · S1-S17 五波重构，行为不变 + 测试守护）✅

**第一波 结构性修复**
- S1 解除 core→api 反向依赖：`AppServices` 数据类外移到 `app/services.py`，core 层零 api 依赖
- S2 拆分 `backup.py`(560行) → `app/core/backup/` 包：specs / engine / runner / scheduler / fsutil，`from app.core.backup import X` 保持兼容
- S3 `core/__init__`、`core/indexer/__init__` 去导入副作用（import app.core 不再加载 jieba 重依赖）
- S4 删死代码 `search._first_token`、归档 `frontend/static/`、清理 src 下 5 个临时脚本

**第二波 重复与魔法值收敛**
- S5 统一解码降级：新增 `core/textcodec.py`，vault 与 indexer 共用（坑 #6）
- S6 pre-write 常量统一：routes_backup 硬编码 `"pre-write"`/`"tree"` → backup 常量与 `pre_write_file()` 方法
- S7 config 三段 split 收敛为 `_split_csv` 公共方法
- S8 前端 API URL 常量集中：新增 `api/endpoints.js`，24+ 处散落 URL 全部收敛

**第三波 API 薄化 + Schemas**
- S9 7 个内联 BaseModel 迁入 schemas.py（backup/agent/settings）
- S10 backup/settings/index 稳定端点挂 response_model（BackupListOut/RunnerStatusOut/IndexStatusOut 等）
- S11 文件系统操作收敛：`browse_dirs` 移入 core/platform，routes_filesystem 变薄；history 改用 `snapshot_files`/`pre_write_file`

**第四波 前端模块化**
- S12 拆分 BackupPanel(450行) → SnapshotList / AutoBackupSettings / HistoryRestore / RestoreAll 4 子组件 + format.js 共享工具，父组件 237 行
- S13 App.vue 抽 composable：`useVaultDocs`（树/文档）、`useSearch`（搜索），App 344→282 行
- S14 统一备份轮询：新增 `useBackupStatus` 单例（BackupPanel 是唯一数据源），消除双轮询同接口与 backupNow 双实现
- S15 移动端修复：`.tree-item`→`.tree-row`（规则原失效）、`.chat-panel` 高度适配底部 Tab、SearchPanel 窄屏

**第五波 测试补全**
- S16 新增 10 用例：browse_dirs(3) / open_in_file_manager / textcodec(4) / pre_write_file / scheduler 空表达式
- S17 全量验收：**pytest 147 passed**；ruff / format / mypy 0 错（35 文件）；前端构建通过

### 全量模块化梳理（2026-08-01 · S1-S17 五波重构，行为不变 + 测试守护）✅

**第一波 结构性修复**
- S1 解除 core→api 反向依赖：`AppServices` 数据类外移到 `app/services.py`，core 层零 api 依赖
- S2 拆分 `backup.py`(560行) → `app/core/backup/` 包：specs / engine / runner / scheduler / fsutil，`from app.core.backup import X` 保持兼容
- S3 `core/__init__`、`core/indexer/__init__` 去导入副作用（import app.core 不再加载 jieba 重依赖）
- S4 删死代码 `search._first_token`、归档 `frontend/static/`、清理 src 下 5 个临时脚本

**第二波 重复与魔法值收敛**
- S5 统一解码降级：新增 `core/textcodec.py`，vault 与 indexer 共用（坑 #6）
- S6 pre-write 常量统一：routes_backup 硬编码 `"pre-write"`/`"tree"` → backup 常量与 `pre_write_file()` 方法
- S7 config 三段 split 收敛为 `_split_csv` 公共方法
- S8 前端 API URL 常量集中：新增 `api/endpoints.js`，24+ 处散落 URL 全部收敛

**第三波 API 薄化 + Schemas**
- S9 7 个内联 BaseModel 迁入 schemas.py（backup/agent/settings）
- S10 backup/settings/index 稳定端点挂 response_model（BackupListOut/RunnerStatusOut/IndexStatusOut 等）
- S11 文件系统操作收敛：`browse_dirs` 移入 core/platform，routes_filesystem 变薄；history 改用 `snapshot_files`/`pre_write_file`

**第四波 前端模块化**
- S12 拆分 BackupPanel(450行) → SnapshotList / AutoBackupSettings / HistoryRestore / RestoreAll 4 子组件 + format.js 共享工具，父组件 237 行
- S13 App.vue 抽 composable：`useVaultDocs`（树/文档）、`useSearch`（搜索），App 344→282 行
- S14 统一备份轮询：新增 `useBackupStatus` 单例（BackupPanel 是唯一数据源），消除双轮询同接口与 backupNow 双实现
- S15 移动端修复：`.tree-item`→`.tree-row`（规则原失效）、`.chat-panel` 高度适配底部 Tab、SearchPanel 窄屏

**第五波 测试补全**
- S16 新增 10 用例：browse_dirs(3) / open_in_file_manager / textcodec(4) / pre_write_file / scheduler 空表达式
- S17 全量验收：**pytest 147 passed**；ruff / format / mypy 0 错（35 文件）；前端构建通过

### 备份管理增强·修订2（2026-08-01 · 删除真修复 + UI 折叠）✅

**修复：删除大快照失败/卡慢（真根因二连）**
- 根因 1：safe-delete 钩子不仅替换 shutil.rmtree，**Python 层 unlink/rmdir 批量删除超过 50 个/次也会被拦**（回收站不可用→拒绝），1770 文件快照删到 50 个就停
- 根因 2：Windows `cmd /c rmdir /s /q` 系统命令**不受钩子拦截**，实测 1770 文件快照 ~10s 删完（对比 Python 逐文件 6 分钟+）
- 修复：`rmtree_manual` 改为**优先调系统命令**（Windows `cmd /c rmdir /s /q`、Linux/macOS `rm -rf`），失败回退 Python 手动删除；删除接口保持异步（202）
- 实测：删除接口 0.4s 返回，8s 内大快照彻底删除（列表+磁盘均干净）

**UI：自动备份说明折叠**
- 说明文字改一行摘要「每 X 分钟自动备份（活跃时触发）」+「详情 ▼」按钮
- 点击展开完整规则（3 条分点），再点「收起 ▲」折叠

**其他**
- 本地后端端口 8080 被 WorkBuddy 托管的僵尸进程占用（TerminateProcess 可清），dev 改用 **8090**，vite proxy 同步更新
- 测试全量通过（137），ruff/mypy/构建全绿

### 备份管理增强·修订2（2026-08-01 · 删除真修复 + UI 折叠）✅

**修复：删除大快照失败/卡慢（真根因二连）**
- 根因 1：safe-delete 钩子不仅替换 shutil.rmtree，**Python 层 unlink/rmdir 批量删除超过 50 个/次也会被拦**（回收站不可用→拒绝），1770 文件快照删到 50 个就停
- 根因 2：Windows `cmd /c rmdir /s /q` 系统命令**不受钩子拦截**，实测 1770 文件快照 ~10s 删完（对比 Python 逐文件 6 分钟+）
- 修复：`rmtree_manual` 改为**优先调系统命令**（Windows `cmd /c rmdir /s /q`、Linux/macOS `rm -rf`），失败回退 Python 手动删除；删除接口保持异步（202）
- 实测：删除接口 0.4s 返回，8s 内大快照彻底删除（列表+磁盘均干净）

**UI：自动备份说明折叠**
- 说明文字改一行摘要「每 X 分钟自动备份（活跃时触发）」+「详情 ▼」按钮
- 点击展开完整规则（3 条分点），再点「收起 ▲」折叠

**其他**
- 本地后端端口 8080 被 WorkBuddy 托管的僵尸进程占用（TerminateProcess 可清），dev 改用 **8090**，vite proxy 同步更新
- 测试全量通过（137），ruff/mypy/构建全绿

### 备份管理增强·修订（2026-08-01 · 删除异步化 + 默认目录 + UI 文案）✅

**修复：删除快照卡住（"一直显示删除中"）**
- 根因：Windows 上删除硬链接文件极慢（约 130ms/个，1770 文件需 ~4 分钟），删除接口同步等待导致前端卡死
- 修复：`DELETE /api/backup/{id}` 改**后台线程删除**，立即返回 202 `{async:true}`；前端标记"删除中"并每 3s 轮询列表，快照消失即完成提示
- 回归：新增 `_wait_snapshots_gone` 测试辅助

**变更：默认备份目录 → 项目根/backups**
- `Settings.resolved_backup_dir` 默认从 `{data_dir}/backups` 改为 **`{项目根}/backups`**——Docker 部署时挂载宿主卷到 `/app/backups` 即可，无需改配置
- 测试 fixture 全部显式指定 `backup_dir=tmp_path/backups` 隔离（否则污染真实备份目录）

**UI：按钮文案 + emoji**
- 「打开备份目录」→「📂 打开目录」；「更换备份目录」→「📝 修改目录」；卡片标题加 emoji
- 自动备份卡片重构：间隔输入 + 「✅ 应用」按钮 + 分点说明文字（原说明/按钮排布杂乱）

**其他**
- 自动备份间隔默认 30 分钟确认（settings.json 已重置为 30）
- 测试更新：`test_resolved_backup_dir_defaults_to_project`（新默认）、删除异步断言

**验证**：pytest 137 passed；ruff/format/mypy 0 错；删除接口 0.2s 返回（原 60s+ 卡死）；测试污染快照已清理

### 备份管理增强·修订（2026-08-01 · 删除异步化 + 默认目录 + UI 文案）✅

**修复：删除快照卡住（"一直显示删除中"）**
- 根因：Windows 上删除硬链接文件极慢（约 130ms/个，1770 文件需 ~4 分钟），删除接口同步等待导致前端卡死
- 修复：`DELETE /api/backup/{id}` 改**后台线程删除**，立即返回 202 `{async:true}`；前端标记"删除中"并每 3s 轮询列表，快照消失即完成提示
- 回归：新增 `_wait_snapshots_gone` 测试辅助

**变更：默认备份目录 → 项目根/backups**
- `Settings.resolved_backup_dir` 默认从 `{data_dir}/backups` 改为 **`{项目根}/backups`**——Docker 部署时挂载宿主卷到 `/app/backups` 即可，无需改配置
- 测试 fixture 全部显式指定 `backup_dir=tmp_path/backups` 隔离（否则污染真实备份目录）

**UI：按钮文案 + emoji**
- 「打开备份目录」→「📂 打开目录」；「更换备份目录」→「📝 修改目录」；卡片标题加 emoji
- 自动备份卡片重构：间隔输入 + 「✅ 应用」按钮 + 分点说明文字（原说明/按钮排布杂乱）

**其他**
- 自动备份间隔默认 30 分钟确认（settings.json 已重置为 30）
- 测试更新：`test_resolved_backup_dir_defaults_to_project`（新默认）、删除异步断言

**验证**：pytest 137 passed；ruff/format/mypy 0 错；删除接口 0.2s 返回（原 60s+ 卡死）；测试污染快照已清理

### 备份管理增强（2026-08-01 · 打开目录 + 删除修复 + 活跃式自动备份）✅

**新增：打开备份目录**
- 后端 `POST /api/settings/backupdir/open`：跨平台调用系统文件管理器打开备份目录（Windows `os.startfile` / macOS `open` / Linux `xdg-open`，见 `core/platform.py open_in_file_manager`）
- 前端备份目录卡片新增「📂 打开备份目录」按钮

**修复：删除快照失败（真根因）**
- 症状：删除接口返回 200 但快照仍存在（"删了但还在"）
- 根因：WorkBuddy 环境 `sitecustomize` 钩子把 `shutil.rmtree` 替换为安全删除版本——需要系统回收站，Windows 沙箱回收站不可用 → 拒绝删除目录；且原代码 `ignore_errors=True` 静默吞掉失败
- 修复：新增 `core/backup.py rmtree_manual()`——手动递归删除（逐文件 `unlink` + 逐空目录 `rmdir`，不受钩子影响）；`delete_snapshot` 与 `cleanup_retention` 均改用它；失败项收集上报（不再静默），重试一次后仍失败抛 BackupError
- 前端：删除按钮加防抖禁用 + 删除成功刷新列表

**新增：活跃式自动备份（替代 cron 定时）**
- 需求：打开页面后，设定间隔 N 分钟；有操作且到间隔才备份，只挂页面不动不备份，关页面即停
- 后端：`Settings.auto_backup_interval_minutes`（默认 30）；`GET/PUT /api/settings/autobackup`（间隔持久化到 settings.json，1~1440 分钟钳制）；`POST /api/backup/now` 支持 `reason=auto`（快照标记来源）；`BACKUP_SCHEDULE` cron 定时**废弃**（.env 已注释，保留字段兼容）
- 前端：`useAutoBackup.js` composable——监听 click/键盘/滚动/触摸记录活动时间，每 30s 检查「距上次备份≥间隔 且 距上次操作≤间隔」→ 调 auto 备份；页面关闭即停止；备份管理页新增「自动备份」卡片（间隔输入+保存）

**验证**：pytest 136 passed；ruff/format/mypy 0 错；删除真实生效（列表与磁盘均移除）；auto 快照正确标记

### 代码梳理 + 移动端适配（2026-08-01 · 大版本更新）✅

**清理**
- 移除 `DebugPanel.vue` 临时滚动诊断组件（滚动问题已根治）；`frontend/static/` 旧版 M0 占位页归档并写 README 标注废弃；修复 docs/07 更新日志重复段落

**后端模块化补全（重构计划 S6 / R1-R4）**
- 新增 `app/schemas.py`：统一请求/响应模型（vault tree/file/meta、search），routes_vault / routes_search 挂 `response_model`，不再裸 dict
- 合并白名单校验：`agent/safety.py` 新增 `check_write_rules`（禁写目录+隐藏路径唯一实现），`deps.ensure_writable` 复用；修正 `..` 不算隐藏路径（保持 422 语义）
- `core/indexer/service.py` 新增 `building` 属性，routes_index 不再访问私有 `_building`；routes_settings 清理未用 `request` 参数
- 新增 `tests/conftest.py`（统一 fixture）+ `tests/test_platform.py`（库检测/挂载点过滤 6 用例）+ `tests/test_llm.py`（provider 映射 6 用例）；`detect_vaults` 修 limit 循环内生效

**前端模块化**
- 新增 `useStatusPolling.js` composable：索引/备份状态轮询 + 定时器生命周期从 App.vue 抽离（App.vue 338 → 311 行）
- 5 个组件（FileTree/DocView/SearchPanel/DirPicker/BackupPanel）补 Props/Emits JSDoc 契约注释

**移动端适配（断点 768px，mobile-first）**
- 侧栏抽屉化：移动端 `translateX(-100%)` 隐藏 + ☰ 汉堡按钮滑入 + 半透明遮罩 + 关闭按钮
- 底部 Tab 导航（文档/搜索/对话/备份），顶部 Tab 移动端隐藏；safe-area-inset-bottom 适配 iOS
- 移动端顶栏：汉堡 + 标题 + 搜索框
- 内容区：padding 紧凑、超宽表格横向滚动、编辑器高度自适应
- 目录选择器双栏改单栏堆叠；BackupPanel `.bp-tr` 列表改卡片式（data-label 字段标签）
- 点击区域最小 44px；<480px 进一步压缩字号

**验证**：pytest 129 passed；ruff 0 / format 0 / mypy 0（28 文件）；前端构建通过；8080/5173 正常；目录树/搜索/快捷位置/备份链路全通

### 模块化重构（S0-S5 · 2026-08-01 · 行为不变 + 测试守护）✅

**重构**
- `app/core/runtime.py`（新）：运行时配置持久化 / 服务构建工厂 / **热切换并发锁 `switch_lock`**（防并发切换竞态）
- `app/core/platform.py`（新）：磁盘枚举（Windows 盘符 / Linux 挂载点）/ Obsidian 库检测 / 快捷位置，纯平台逻辑无 Web 依赖
- `app/api/routes_filesystem.py`（新）：browse / detect / quickaccess 薄路由
- `app/api/routes_settings.py`：**497 → 182 行**——只保留 vault / backupdir 查看与热切换（热切换包并发锁）
- `app/main.py`：lifespan 启动/关闭/后台线程统一从 `app.state.services` 动态取服务（修复闭包引用旧对象隐患）；dist 缺失时明确报错不静默回退旧版
- 前端 `useDirPicker.js`（新）composable + `components/DirPicker.vue`（新）通用目录选择器：App.vue（439→331）与 BackupPanel.vue（466→364）共用一份目录选择逻辑

**修复**
- `test_bad_config_placeholder`：改为显式构造无效 Settings 调 create_app，不再依赖全局 .env
- **深层目录文件打不开（Vue 递归组件事件转发 bug）**：`FileTree.vue` 递归子组件用 `@open="openFile"` 转发事件——子层 emit 的已是 `path` 字符串，父层 `openFile(node)` 却把它当 node 对象再取 `.path` → `undefined` → 请求 `path=undefined` 404。根目录文件不经递归转发所以正常。修复：递归处改为 `@open="(p) => emit('open', p)"` 透传（与 `@load-dir` 一致），深层文件可正常打开

**优化**
- **滚动条跳动【真正根因修复】（v6）**：诊断面板数据揭示 `#app` 子元素列表出现 `div#app`（双层嵌套）——index.html 挂载点 `#app` 与 App.vue 模板根 `#app` **id 重复**，Vue 渲染出 外层挂载点(2430 宽) 包 内层根（被 flex 收缩到内容宽度 1010），右侧 1420px 空白、主区宽度 随内容跳变（284/438/710）→ 表现为「滚动条跳动」。修复：App.vue 根改 `<div class="app-root">`， styles.css 选择器同步改 `.app-root`。**结论：有滚动条是文档应用正常形态；几百 px 跳动是 双 id 嵌套 bug，与文字宽度无关，已根治**
- **主区布局重写定稿（v4，停止打补丁，回归业界标准）**：按 ExpertBeacon flexbox 固定侧栏教程 / codestudy 双栏独立滚动指南重写。① `html, body { overflow: hidden }` 锁定视口（标准做法）；② `#sidebar { flex-shrink: 0 }` 固定宽不被压缩；③ **`#main { min-width: 0 }`** —— flex 子项防被超宽内容（长表格/长代码）撑宽，这是内容宽度稳定的关键；④ `#main { overflow-y: scroll }` 主区滚动条常驻——文档应用（Obsidian/Notion/语雀）主区本就有滚动条属正常形态，常驻=宽度恒定=永不跳动；⑤ 删除 scrollbar-gutter 补丁、不自定义滚动条。**结论：「有滚动条」不是 bug，是文档应用正常形态；要防的是「跳动」，已用常驻滚动条+min-width:0 根治**

**测试**
- 新增 `tests/test_runtime.py` 13 用例：vault/backupdir 切换（成功/失败/unchanged/持久化/坑#11 拒绝）+ browse/detect/quickaccess + **并发切换回归**；全量 **116 passed**

### 新增：界面切换库路径（2026-08-01 · 本地调试版）✅

**新增**
- `app/api/routes_settings.py`：`GET/POST /api/settings/vault` —— 运行时查看 / **热切换** vault 路径
  - 切换流程：停 watcher/备份调度 → 关闭旧索引库 → 重建全套服务（vault/index/search/backup/agent）→ 重启 watcher/调度 → 后台全量重建索引
  - 持久化到 `{data_dir}/settings.json`，重启后自动生效（`load_runtime_settings`）
  - 校验：路径必须存在、备份目录不得位于新 vault 内（坑 #11）
- `app/api/routes_settings.py`：`GET /api/settings/browse` —— 目录浏览接口（前端资源管理器式选择器用）：空 path 返回盘符列表，非空返回父目录+子目录名，无权限目录容错跳过
- `app/api/routes_settings.py`：`GET /api/settings/detect` —— **自动检测本机 Obsidian 库**：扫描用户目录/桌面/文档/下载等常见位置（限深 5 层防卡顿），以 `.obsidian` 目录为库标志，返回 [{path,name,mdCount}]；Windows 上跳过 `/Users` 等相对路径候选避免重复项
- `app/api/routes_settings.py`：`GET /api/settings/quickaccess` —— **快捷位置**：返回 `{disks, places}` 两组——Windows 枚举盘符（C:/D:/…）+ `SHGetFolderPathW` 取桌面/文档/下载等真实路径（兼容 OneDrive 重定向）；Linux（含 **iStoreOS/群晖**）读 `/proc/mounts` 枚举真实磁盘挂载点（过滤 overlay/tmpfs/proc 等伪文件系统，保留 `/mnt/sda1`、`/mnt/usb`、`/volume1` 等），非 Windows 常用位置回退 `~/Desktop` 等约定路径
- 前端 `App.vue`：侧边栏新增「当前库路径 + 切换库按钮」；切换弹窗集成**自动检测库列表（一键切换）** + **资源管理器式双栏目录浏览器（左侧快捷位置：桌面/文档/下载/图片，右侧目录树）** + 手动输入；切换中按钮禁用显示「切换中…」，成功后提示重建索引

**修复**
- `app/main.py` `/api/health`：闭包引用切换前的旧 index 对象 → vault 热切换后 health 报 `sqlite3.ProgrammingError: Cannot operate on a closed database`。改为从 `request.app.state.services` 动态取服务；lifespan 关闭逻辑同步改为取最新服务

**优化**
- **搜索结果高亮**：`app/core/search.py` snippet 新增 `hitWords`（片段内实际命中的查询词，含 jieba 分词词，整串优先）；前端 `SearchPanel.vue` 用 `<mark>` 渲染高亮（先转义防 XSS、长词优先替换防嵌套）
- **修复层级目录无法访问**：根因是 8080 端口在 `frontend/dist` 缺失时回退挂载 `frontend/static` 旧版前端（M0 占位页），其文件树不支持层级访问。修复：构建 Vue 前端产物到 `dist`，8080 直接服务完整版；同时 FileTree 懒加载补 `childrenLoaded` 标志（App.loadDir 设置，避免重复请求与竞态）
- **英文大小写不敏感搜索与高亮**：检索层 FTS5 unicode61 默认对 ASCII 大小写不敏感，但 snippet 定位 `find()` 与前端高亮替换是大小写敏感的 → 搜小写 `obsidian` 原文 `Obsidian` 无法高亮。修复：后端新增 `_find_ci()`（lower 定位）、hit_words 用 `lower()` 匹配；前端 SearchPanel 与 DocView 正文高亮改用正则 `gi`（保留原文大小写显示、正文所有命中都高亮并滚动定位首个）
- **主色调改为 #ff9292**：accent / 高亮 mark 统一改为用户指定的粉色系；因 #ff9292 是亮色，accent 背景上的文字（主按钮、聊天气泡、mark 高亮）从白色改为深色 `--on-accent`/`--mark-text` 保证可读性；新增 `--accent-soft`（粉调半透明背景）

**新增：备份管理页面**
- 前端新增 `components/BackupPanel.vue` + 主区「备份管理」Tab：概览卡片（备份状态/快照统计/当前库/操作）、单文件历史恢复（按相对路径查询各快照+写前备份版本，一键恢复）、快照列表（时间/原因/文件数/大小/校验状态 + 校验/查看文件/删除）、整库恢复（危险区，需输入确认码 RESTORE）；10s 轮询捕获定时备份状态
- 后端 `routes_backup.py` 新增 `GET /api/backup/files?snapshotId=`（快照内文件列表）与 `POST /api/backup/verify/{snap_id}`（快照校验：文件数+抽样 checksum）
- **备份目录可界面切换**：后端 `GET/POST /api/settings/backupdir` —— 热切换备份目录只重建 backup 相关服务（engine/runner/scheduler/agent），不动 vault/index（避免重索引）；校验目录存在、不得位于 vault 内（坑 #11）；持久化 `backup_dir` 到 settings.json（与 vault_path 并存，启动自动加载）。前端 BackupPanel 概览新增「备份目录」卡片 + 更换弹窗（复用快捷位置/目录浏览器选择器）

**变更**
- `app/main.py` 服务构建逻辑抽为 `routes_settings._build_services()`，`create_app` 与热切换共用（行为一致）

### 已实现（2026-08-01 · GitHub Actions CI 打通 + 单 commit 推送 + 关键 bug 修复）✅

**新增**
- `.github/workflows/build.yml`：仿 learning-platform 的 CI（push main 触发）——test job（pytest 99 用例）+ build job（构建并推送 `ghcr.io/simiely/obsidian-agent:latest`），concurrency 策略取消重叠运行
- `scripts/upload_github.py` **重写为 Git Data API 单 commit 批量提交**（blob→tree→commit→ref 四步）：N 个文件 = 1 个 commit = **只触发 1 次 Actions 构建**（原逐文件 Contents API 每文件一个 commit，实测产生 128 个 run）

**修复（CI 实测抓到的真实 bug）**
- **单 sqlite 连接跨线程并发 → Linux 段错误**（`fts5.py build()` segfault，本地 Windows 测不出）：加 `threading.RLock` 串行化全部连接操作 + 新增并发回归测试（坑 #6.4）
- **Dockerfile `pip install .` 缺 README.md**（pyproject 声明 readme 但镜像没 COPY）：补 `COPY README.md ./`
- 镜像包 `ghcr.io/simiely/obsidian-agent` 已存在且 **public**（服务器免登录可拉）

**验证**：Actions 全绿（test 99 passed + build 成功推送镜像）✅；GHCR 包 2 个版本 public ✅

**变更**
- `docker-compose.yml` 重写为**预构建镜像部署版**（仿 learning-platform 风格）：
  - `image: ghcr.io/simiely/obsidian-agent:latest`（无需服务器构建）
  - `user: root`（规避 host mount 下 SQLite 权限问题，内网最省事）
  - `pull_policy: always`（Dpanel「更新」= restart 时自动拉新镜像）
  - 数据路径：`/mnt/usb2/Configs/obsidian-agent/{vaults,data}`；宿主机端口 `2512:8080`
- 新增 `scripts/build-push.sh`：构建并推送镜像到 GHCR（PAT 权限 write:packages）
- `docs/03-快速开始.md`：拆分「本机构建 A1」与「预构建镜像 A2」两种部署方式
- `docs/08-踩坑记录.md`：新增 3 条 CI 实测坑（sqlite 段错误 / README 缺失 / 逐文件推送刷屏）

**验证**：compose YAML 语法 ✅ / build-push.sh bash 语法 ✅（沙箱无 docker，镜像构建需在用户机器执行）

### 已实现（2026-08-01 · R0：工具链落地）✅

**变更（无行为变化，工程卫生）**
- ruff 配置 `extend-immutable-calls`（精确豁免 FastAPI Depends/Query/Header 标准模式，而非整体忽略 B008）；全库 `ruff format` 统一格式
- **ruff 81 → 0**：E501 断行 / B007 循环变量 / E402 import 位置 / B017 盲异常断言等全部清零
- **mypy 78 → 0**：routes 层返回类型 `dict[str, Any]` 化、sse_gen/lifespan/_deps_for 补注解、IndexBackend Protocol 对齐、`Callable`/`Iterator`/`cast` 导入补齐
- 新增一次性工具 `scripts/fix_mypy_annotations.py`（泛型参数批量修复，未来可复用）
- **顺带修复潜在 bug**：`_tokenize_fields(doc.title, *doc.aliases, body)` 参数展开（原误传 list，aliases 非空时 `tokenize(list)` 会崩）

**验证**：ruff 0 + mypy 0 + **pytest 98/98 全绿**

### 已实现（2026-07-31 · M5：AI Agent 集成）✅

**新增**
- `app/agent/llm.py`：pydantic-ai 2.x 模型配置层（内置 provider：deepseek/openai/moonshotai/ollama；api_key 经环境变量注入；`is_configured` 守卫）
- `app/agent/safety.py`：写前安全层（路径白名单/禁写目录/备份/diff 截断）——与框架无关，可独立测试
- `app/agent/service.py`：AgentService（Pydantic AI Agent 构建 + 4 个类型化工具 + SSE 事件队列）+ **PendingStore 暂存式写操作**（HITL：确认前不落盘，30 分钟 TTL 清理）
  - 工具：`read_file` / `search`（中文分词检索）/ `list_tree` / `write_file`（**暂存待确认**，不直接写盘）
- `app/api/routes_agent.py`：POST /chat（SSE：session/text/tool/done/error 事件）、POST /confirm、POST /cancel、GET /session
- 前端 `ChatPanel.vue`：SSE 流式对话 + 写操作确认框（diff 展示 + 确认/取消），App.vue 增加"Agent 对话"标签页
- 测试：test_agent.py（11 用例：safety/pending/工具/守卫）+ test_api.py Agent 4 用例

**验证**
- pytest **98/98 全绿**（累计 M0-M5）
- **M5 端到端冒烟 PASS**（scripts/m5_smoke.py，FunctionModel 模拟 LLM）：工具调用 → SSE tool 事件（含 diff）→ 确认前不落盘 → confirm 写入+索引同步 → cancel 不写入
- 真实 LLM 调用需用户环境 API key（沙箱无 key，以 FunctionModel 验证链路）

### 规划中（Roadmap 摘要，详见 02-架构设计.md）

- [x] M0：Docker 骨架（compose / 镜像 / 配置加载）
- [x] M1：Vault 访问核心（目录树 / 读写 / watchdog 监听 / 忽略规则 / 备份引擎）
- [x] M2：全文索引与检索（SQLite FTS5 + jieba，中文分词，增量更新）
- [x] M3：后端 API 层（vault / search / index / backup / agent 五组路由）
- [x] M4：前端浏览页面（Vue3 四视图 + 工程化构建）
- [x] M5：AI Agent（LLM 适配 / 工具集 / HITL 写前确认 / 写前备份）
- [ ] M6：测试补全、文档完善、v0.1.0 发布

---

## 0.1.0 - 待发布（首个 MVP）

### 新增
- 首次发布。功能基线见 [docs/01-功能说明.md](01-功能说明.md) 中 ✅ 标记的全部功能。

---

## 版本记录格式模板（开发者发布时复制使用）

```markdown
## [x.y.z] - YYYY-MM-DD

### 新增
- ...

### 修复
- ...

### 变更
- ...

### 移除
- ...

### 文档
- ...
```
