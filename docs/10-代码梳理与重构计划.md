# 10 - 代码梳理与重构计划

> 本文档是 M0-M5 完成后的**全量代码梳理**：现状模块地图、静态检查硬证据、最佳实践差距、重构计划（R0-R6）。
> 目标：消除重复逻辑与类型隐患，让代码库长期可维护。

---

## 1. 现状梳理（2026-07-31 完成时快照）

### 1.1 代码规模

| 范围 | 行数 | 说明 |
|---|---|---|
| `app/` 后端 | ~2100 | core 领域 + agent + api + config + main |
| `tests/` | ~1400 | 98 用例全绿 |
| `scripts/` | ~400 | 冒烟/上传脚本 |
| `frontend/` | ~1100 (Vue) | 5 组件 + md.js + styles |

### 1.2 模块地图与依赖方向

```
app/main.py  create_app 工厂（组装服务 + 挂路由 + 异常映射 + lifespan）
   │
   ├── app/api/*         路由层（薄 HTTP 适配，依赖注入 AppServices）
   │     deps.py         AppServices / require_auth / ensure_writable
   │     routes_*.py     vault · search · index · backup · agent
   │
   ├── app/agent/*       AI 层（Pydantic AI）
   │     llm.py 模型配置 · safety.py 写前安全 · service.py AgentService+PendingStore
   │
   ├── app/core/*        领域层（不依赖 Web 框架）
   │     vault.py   Vault 文件门面 + VaultWatcher（~300 行）
   │     backup.py  BackupEngine/Runner/Scheduler/Cron/Retention（~400 行）
   │     markdown.py frontmatter/标题/wikilink 解析
   │     indexer/   base(协议) + fts5(实现) + service(编排)
   │     search.py  SearchService（检索结果包装）
   │
   └── app/config.py     全部配置（pydantic-settings）

frontend/src/*   Vue3：App + FileTree/DocView/SearchPanel/ChatPanel + md.js
```

**依赖单向性已验证**：`api → agent/core → (config)`，core 不 import Web 框架 ✅（这是最大的架构资产，重构时保持）。

### 1.3 静态检查硬证据（重构依据）

| 工具 | 问题数 | 主要类别 |
|---|---|---|
| ruff | **81** | E501 行过长 23 · **B008 函数默认参数调函数 20**（FastAPI `Depends()` 标准模式，需配置忽略）· UP012 19 · F401 未用 import 6 · 其余 13 |
| mypy | **78** | **`dict` 缺类型参数 60+**（routes 层应改 `dict[str, Any]`）· sse_gen/内部函数缺返回注解 · 少数未标注 |

> **结论：逻辑健康（98 测试全绿），工程卫生欠账**——81+78 项全是风格/类型问题，无逻辑错误，但会拖慢后续维护。

## 2. 最佳实践参照（2026 分层架构标准）

调研结论（fastapi 官方 / pythonz2h / 企业级脚手架指南 2026）：

| 最佳实践 | 本项目现状 | 差距 |
|---|---|---|
| 分层 Routes → Services → Repositories | ✅ api 薄路由 + core 服务层 | core 内 vault/index/backup 兼具 services+repositories 角色，命名已可读，**不强行拆分** |
| 路由只做 HTTP 适配 | ✅ 已做到 | - |
| **Schemas/DTO 层**（请求/响应类型化） | ⚠️ 请求模型内联在 routes，**响应用裸 dict** | **R1：建 `api/schemas.py`，响应类型化**（同时解决 mypy 60+ 项） |
| 集中配置（Pydantic Settings v2） | ✅ config.py | - |
| 依赖注入 | ✅ Depends + AppServices | - |
| 自定义异常集中定义 | ⚠️ 异常散在 core（VaultError/BackupError/SafetyError），映射在 main | **R3：集中到 `core/exceptions.py` 或维持现状并文档化** |
| 工具链（lint/type 全绿） | ❌ 未跑通 | **R0** |
| DRY（重复逻辑消除） | ⚠️ 白名单校验两处重复 | **R2** |

## 3. 重复逻辑与隐患清单（重构重点）

| # | 位置 | 问题 | 处理 |
|---|---|---|---|
| D1 | `api/deps.ensure_writable` vs `agent/safety.check_writable` | **同一白名单校验两套实现**，未来规则漂移风险 | R2：合并到 `core/security.py`（或 vault 静态方法），两处调用 |
| D2 | 后端 `markdown.py` vs 前端 `md.js` | frontmatter/wikilink 语法解析**跨语言重复**（两套实现） | R5：**一致性对照表文档化**（语法规则、边界行为），不强行统一代码（成本高），但锁死行为差异 |
| D3 | `SearchService`（core/search.py） | 只是 IndexService 的薄包装（2 个方法转发） | R3：合并进 IndexService 或标注为展示层适配——建议合并，减少一层间接 |
| D4 | routes_agent `_deps_for` | 为 confirm 构造 AgentDeps，有点别扭 | R2：PendingStore.apply 改接收显式参数（vault/index/backup/settings）而非整个 deps |
| D5 | `app/core/__init__.py` | `from app.core import backup, vault` 导入副作用 | R3：移除，改显式导入 |

## 4. 重构计划（R0-R6）

> 原则：**行为不变（98 测试守护）、小步提交、每步有验收**。逻辑层不重构结构，只清理卫生。

### R0 工具链落地（无行为变化）✅ 2026-07-31 完成
- [x] ruff 配置：`[tool.ruff.lint.flake8-bugbear] extend-immutable-calls`（精确豁免 FastAPI Depends/Query/Header 标准模式）→ `--fix` 自动修 + 手动修 E501/B007/E402/B017
- [x] mypy：routes 层 `dict` → `dict[str, Any]`（脚本 `scripts/fix_mypy_annotations.py` 批量 + 手动）、补 sse_gen/lifespan/_deps_for 等注解
- [x] **顺带发现并修复潜在 bug**：`_tokenize_fields(doc.title, doc.aliases, body)` 误传 list（应 `*doc.aliases` 展开），aliases 非空时会导致 `tokenize(list)` 崩溃——测试未覆盖，R0 修复
- **验收：✅ ruff 0 错误 + mypy 0 错误 + pytest 98 全绿**（159 项 → 0）

### R1 API Schemas 层（响应类型化）
- [x] `app/schemas.py`（已建，含 vault/search 模型，2026-08-01 完成）
- [x] routes_vault / routes_search 已挂 response_model
- **验收**：mypy routes 0 错误；OpenAPI 文档更精确；pytest 绿

### R2 重复逻辑消除
- [x] 白名单校验合并：`agent/safety.check_write_rules` 唯一实现，deps.ensure_writable 复用（2026-08-01 完成）
- [ ] `PendingStore.apply` 签名简化（显式传 vault/index/backup/settings）
- **验收**：无两处重复实现；tests/test_agent + test_api 相关用例绿

### R3 core 职责微调
- [ ] `SearchService` 合并进 `IndexService`（未做，保留薄适配）
- [ ] `core/__init__.py` 移除导入副作用
- [ ] （可选）新增 `core/exceptions.py` 集中领域异常，main.py 映射保持不变
- **验收**：依赖图更清晰；pytest 绿

### R4 前端卫生
- [x] 组件 Props/Emits 补 JSDoc 契约注释（5 组件，2026-08-01）；`defineProps<{...}>` TS 化未做（纯 JS 项目）
- [ ] （可选）加 eslint + prettier 配置
- **验收**：`npm run build` 通过；无类型告警

### R5 前后端语法一致性文档
- [ ] `docs/11-语法一致性对照.md`：后端 markdown.py ↔ 前端 md.js 对 frontmatter/wikilink/callout/高亮/注释 的解析规则逐条对照 + 已知差异
- **验收**：文档覆盖全部已实现语法；差异点有明确理由

### R6 CI 就绪 + 归档
- [ ] `scripts/check.sh`（或 Makefile）：ruff → mypy → pytest 一键全跑
- [ ] （可选）GitHub Actions workflow（push 触发 check）
- [ ] 07-CHANGELOG 归档 v0.1.0；02-架构设计 更新最终目录结构
- **验收**：一条命令完成全量检查；v0.1.0 发布

## 5. 明确不做的事（避免过度设计）

- ❌ 不引入 repository 层抽象（本项目无数据库 ORM，SQLite 已封装在 core）
- ❌ 不拆分 core 为 services/repositories 两层（模块已足够内聚，拆分徒增间接）
- ❌ 不合并前后端 markdown 解析为单一实现（跨语言成本高，用文档锁行为）
- ❌ 不动 M0-M5 已验证的分层与依赖方向（那是最大资产）

---

# 附录 A：2026-08-01 二次梳理（本地调试期功能增补后）

> 本地调试期新增了大量功能（热切换 vault / 备份目录、目录浏览器、库自动检测、磁盘枚举、备份管理页、搜索高亮、主色调），
> 代码规模与结构发生显著变化，**R0-R6 计划需按本节更新**。

## A.1 现状快照（2026-08-01 实测）

| 范围 | 行数 | 变化 |
|---|---|---|
| `app/` 后端业务代码 | ~3134 | 新增 `routes_settings.py`(497)、`routes_backup` 扩至 126、main.py 201 |
| `frontend/src/` | ~1406 | 新增 `BackupPanel.vue`(466)，App.vue 439、md.js 183 |
| `tests/` | ~1251 | 99 用例：98 通过 / 1 失败（test_bad_config_placeholder 环境预期差异，非代码缺陷） |

**静态检查（实测）**：mypy 0 错误 ✅；ruff 7 错误 ❌（6 个 E501 行过长集中在 routes_settings.py/main.py 新增代码 + 1 个 I001 import 排序）。

## A.2 新增问题清单（重构重点，按优先级）

| # | 位置 | 问题 | 处理 |
|---|---|---|---|
| N1 | `routes_settings.py` 497 行 | **职责过载**：一个文件塞了「运行时配置持久化 / 服务构建工厂 / vault 切换 / 备份目录切换 / 目录浏览 / 库自动检测 / 磁盘枚举(Windows+Linux) / 快捷位置」8 类逻辑 | ✅ S1：拆 `core/runtime.py` + `core/platform.py` + `routes_filesystem.py`，497→182 行 |
| N2 | `App.vue` 439 + `BackupPanel.vue` 466 | **前端目录选择器逻辑重复两份**（quickDisks/quickPlaces/browseTo/enterDir/goQuick/pickCurrent 几乎相同），App.vue 还混有切换库弹窗 | ✅ S2：抽 `DirPicker.vue` + `useDirPicker.js`，两处复用（App 331 / BackupPanel 364） |
| N3 | `main.py` 闭包 `_bootstrap` 引用 `index`/`backend` | lifespan 内闭包持有启动时对象（已修复 health，但 bootstrap 线程仍闭包引用旧对象，vault 热切换后若全量重建并发会串库） | ✅ S3：启动/关闭/后台线程统一从 `app.state.services` 动态取服务 |
| N4 | `routes_settings.py` 内含 `_build_services` 工厂 | 服务工厂放路由文件，职责错位；`create_app` 与热切换两处构建逻辑虽已复用但位置不合理 | ✅ 随 S1 解决（工厂移入 `core/runtime.py`，并加 `switch_lock` 并发锁） |
| N5 | `tests/` 无 settings/backupdir 测试 | 新增的 browse/detect/quickaccess/backupdir/vault 热切换接口**零测试覆盖** | ✅ S5：新增 `tests/test_runtime.py`（13 用例，全量 116 passed） |
| N6 | `test_bad_config_placeholder` 依赖全局无 .env | 本地配了 VAULT_PATH 导致断言 200≠500；测试设计依赖环境 | ✅ S5：改为显式构造 `Settings(vault_path=不存在)` 调 `create_app()` 断言抛错 |
| N7 | `frontend/static/` 旧版占位页仍被挂载 | 实测 8080 在 dist 缺失时回退 static 旧版 → 层级目录无法访问（已通过构建 dist 修复，但回退逻辑仍在，易复发） | ✅ S4：main.py dist 缺失时 `logger.error` 明确提示，不静默回退（旧版归档待后续） |

## A.3 模块化重构计划（S0-S6，行为不变 + 测试守护）

> 原则延续 R 系列：**每步可独立验证、小步提交、pytest/mypy/ruff 全绿为验收**。优先级 N1>N2>N3>N5>N4>N6>N7。

### S0 卫生清零（无行为变化）
- [x] ruff `--fix` + 手动修 E501：routes_settings.py 6 处超 100 列、main.py import 排序（I001）
- [x] 补 `tests/test_runtime.py` 骨架（先写用例再重构，守护行为）
- **验收**：ruff 0 错误；pytest 全绿（含新增用例）✅

### S1 后端 settings 模块化拆分（核心）
- [x] 新建 `app/core/runtime.py`：`load_runtime_settings` / `persist_settings` / `build_services` / `AgentServiceFactory`（纯领域无 HTTP，**新增 `switch_lock` 并发锁**）
- [x] 新建 `app/core/platform.py`：`win_special_folder` / `win_disks` / `linux_disks` / `detect_vaults` / `list_roots` / `quick_access` / `detect_local_vaults`（磁盘与库探测，纯平台逻辑）
- [x] `routes_settings.py` 瘦身为仅 `GET/POST /api/settings/vault` + `GET/POST /api/settings/backupdir`（薄路由，调 runtime 层，热切换包 `switch_lock`）
- [x] 新建 `routes_filesystem.py`：`browse` / `detect` / `quickaccess` 三个接口（薄路由调 platform 层）
- **验收**：接口行为不变；mypy/ruff/pytest 全绿；routes_settings 由 497 行降至 **182** 行 ✅（实测 497→182，接口回归全通过）

### S2 前端目录选择器组件化
- [x] 新建 `components/DirPicker.vue`：通用目录选择弹窗（props: show/title/modelValue/quickDisks/browse*；emits: confirm/browse/quick/enter/pick）
- [x] 新建 `useDirPicker.js` composable：目录选择全部状态与逻辑（open/close/goQuick/enterDir/pickCurrent/browseTo），一份实现两处复用
- [x] App.vue 切换库弹窗、BackupPanel 更换备份目录弹窗均改用 DirPicker + useDirPicker
- [x] `App.vue` 瘦身：目录浏览相关 state/函数全部移除
- **验收**：功能不变；`npm run build` 通过 ✅（App.vue 439→331、BackupPanel 466→364，构建通过）

### S3 热切换并发安全
- [x] `main.py` lifespan `_bootstrap` 线程改为从 `request.app.state.services`（或闭包参数化）动态取最新服务
- [x] 为 vault 热切换 + 全量重建并发补一个回归测试（构造慢索引场景）
- **验收**：切换后 health/tree 不报 closed database；新用例绿 ✅

### S4 静态托管加固
- [x] `main.py` 挂载逻辑：dist 不存在时 `logger.error` 明确提示并返回占位错误页（不静默回退 static 旧版）；或移除 static 回退
- [ ] 删除/归档 `frontend/static/` 旧版（或加 README 标注已废弃）
- **验收**：dist 缺失时日志明确；8080 始终服务 dist ✅（static 归档项待后续）

### S5 测试补齐
- [x] `tests/test_runtime.py`：vault 切换 / 备份目录切换（成功+失败+持久化）/ browse / detect / quickaccess / backupdir 全接口
- [x] 修复 `test_bad_config_placeholder`：显式构造无效 `Settings` 调 `create_app()` 断言占位
- **验收**：新增用例全绿；覆盖率提升（settings 接口 >90%）✅（13 个新用例，全量 116 passed）

### S6 执行原 R1-R6 剩余项（衔接既有计划）
- [ ] R1 API Schemas 层（`api/schemas.py` 响应类型化）
- [ ] R2 重复逻辑消除（白名单校验合并到 `core/security.py`；PendingStore.apply 签名简化）
- [ ] R3 core 职责微调（SearchService 合并进 IndexService；`core/__init__.py` 去副作用）
- [ ] R4 前端类型化（Props/Emits 类型化 + api.js JSDoc）
- [ ] R5 前后端语法一致性文档（docs/11）
- [ ] R6 CI 就绪（scripts/check.sh 一键 ruff→mypy→pytest）+ 归档 v0.1.0
- **验收**：每步独立完成；最终 `./scripts/check.sh` 一条命令全绿

## A.4 推荐执行顺序与风险

| 步骤 | 风险 | 说明 |
|---|---|---|
| S0 → S1 → S5 | 低 | 纯代码搬运 + 测试先行，行为不变 |
| S2 → S3 | 中 | 前端重构靠手动验证；并发靠回归测试守护 |
| S4 → S6 | 低→中 | 清理为主；Schemas 层改动面较大放最后 |

> 建议：先完成 S0+S1+S5（后端模块化+测试固化），再 S2（前端复用），S3-S4 穿插，S6 按原 R1-R6 顺序推进。
> 每步独立提交（`refactor(settings): ...`），可用 `git diff --stat` 验证改动范围可控。

### S1-S17 完成（2026-08-01 · 全量模块化梳理）
- S1 AppServices 外移 app/services.py（core 不再依赖 api）
- S2 backup.py 拆包（specs/engine/runner/scheduler/fsutil）
- S3 __init__ 去副作用；S4 删死代码
- S5 textcodec 统一解码；S6 pre-write 常量统一；S7 config split 收敛；S8 前端 endpoints.js
- S9 内联 BaseModel 迁 schemas；S10 稳定端点挂 response_model；S11 文件操作收敛 core
- S12 BackupPanel 拆 4 子组件；S13 App.vue 抽 composable；S14 useBackupStatus 单例；S15 移动端修复
- S16 补 10 测试；S17 验收（pytest 147；ruff/mypy 0）

### S25 前端编排层瘦身（2026-08-02 · App.vue 410→316 / md.js 拆分 / TOC 抽取）
- S25a App.vue 内联逻辑下沉 composables：`useHashRouter`（路由）、`useVaultSwitcher`（库切换）、`useCreateDoc`（新建）——App.vue 纯编排
- S25b `md.js` 拆出 `mdEnhance.js`（DOM 增强：callout/任务列表/搜索高亮 + escapeRegex），渲染与 DOM 关注点分离（371→299 行）
- S25c DocView 抽 `useToc.js`（点击跳转 + 滚动高亮双机制），DocView 270→217 行
- S25d 清理临时垃圾文件 `frontend/src/_mem2.py`
- 验收：构建通过；CDP 回归（文档打开/hash 路由/TOC 130 项点击高亮/新建对话框/备份 tab）全通过
