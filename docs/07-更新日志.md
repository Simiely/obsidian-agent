# 07 - 更新日志（CHANGELOG）

> 记录每一次版本变更的**更新内容**。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。
> 每个版本的条目由开发者在发布时从开发日志汇总而来，按「新增 / 修复 / 变更 / 移除 / 文档」分类。

---

## 版本规则

- `x.y.z`：主版本（不兼容 API 变更）· 次版本（向后兼容的新功能）· 补丁（向后兼容的 bug 修复）
- 未发布内容记录在 **Unreleased** 段，发布时移动到具体版本号
- 每次用户可见的变更（功能、修复、配置项）都必须记入

---

## Unreleased

### 已实现（2026-08-01 · 部署方式：预构建镜像 + R0 推送 GitHub）✅

**变更**
- `docker-compose.yml` 重写为**预构建镜像部署版**（仿 learning-platform 风格）：
  - `image: ghcr.io/simiely/obsidian-agent:latest`（无需服务器构建）
  - `user: root`（规避 host mount 下 SQLite 权限问题，内网最省事）
  - `pull_policy: always`（Dpanel「更新」= restart 时自动拉新镜像）
  - 数据路径：`/mnt/usb2/Configs/obsidian-agent/{vaults,data}`；宿主机端口 `2512:8080`
- 新增 `scripts/build-push.sh`：构建并推送镜像到 GHCR（PAT 权限 write:packages）
- `docs/03-快速开始.md`：拆分「本机构建 A1」与「预构建镜像 A2」两种部署方式
- **R0 全部改动已推送 GitHub**（69/69：67 更新 + 2 新增；upload_github.py 增强为"存在则取 sha 更新"）

**验证**：compose YAML 语法 ✅ / build-push.sh bash 语法 ✅（沙箱无 docker，镜像构建需在用户机器执行）

### 已实现（2026-08-01 · R0：工具链落地）✅

**变更（无行为变化，工程卫生）**
- ruff 配置 `extend-immutable-calls`（精确豁免 FastAPI Depends/Query/Header 标准模式，而非整体忽略 B008）；全库 `ruff format` 统一格式
- **ruff 81 → 0**：E501 断行 / B007 循环变量 / E402 import 位置 / B017 盲异常断言等全部清零
- **mypy 78 → 0**：routes 层返回类型 `dict[str, Any]` 化、sse_gen/lifespan/_deps_for 补注解、IndexBackend Protocol 对齐、`Callable`/`Iterator`/`cast` 导入补齐
- 新增一次性工具 `scripts/fix_mypy_annotations.py`（泛型参数批量修复，未来可复用）
- **顺带修复潜在 bug**：`_tokenize_fields(doc.title, *doc.aliases, body)` 参数展开（原误传 list，aliases 非空时 `tokenize(list)` 会崩）

**验证**：ruff 0 + mypy 0 + **pytest 98/98 全绿**

### 已实现（2026-07-31 · M5：AI Agent 集成）✅

**新增**
- `app/agent/llm.py`：pydantic-ai 2.x 模型配置层（内置 provider：deepseek/openai/moonshotai/ollama；api_key 经环境变量注入；`is_configured` 守卫）
- `app/agent/safety.py`：写前安全层（路径白名单/禁写目录/备份/diff 截断）——与框架无关，可独立测试
- `app/agent/service.py`：AgentService（Pydantic AI Agent 构建 + 4 个类型化工具 + SSE 事件队列）+ **PendingStore 暂存式写操作**（HITL：确认前不落盘，30 分钟 TTL 清理）
  - 工具：`read_file` / `search`（中文分词检索）/ `list_tree` / `write_file`（**暂存待确认**，不直接写盘）
- `app/api/routes_agent.py`：POST /chat（SSE：session/text/tool/done/error 事件）、POST /confirm、POST /cancel、GET /session
- 前端 `ChatPanel.vue`：SSE 流式对话 + 写操作确认框（diff 展示 + 确认/取消），App.vue 增加"Agent 对话"标签页
- 测试：test_agent.py（11 用例：safety/pending/工具/守卫）+ test_api.py Agent 4 用例

**验证**
- pytest **98/98 全绿**（累计 M0-M5）
- **M5 端到端冒烟 PASS**（scripts/m5_smoke.py，FunctionModel 模拟 LLM）：工具调用 → SSE tool 事件（含 diff）→ 确认前不落盘 → confirm 写入+索引同步 → cancel 不写入
- 真实 LLM 调用需用户环境 API key（沙箱无 key，以 FunctionModel 验证链路）

### 规划中（Roadmap 摘要，详见 02-架构设计.md）

- [x] M0：Docker 骨架（compose / 镜像 / 配置加载）
- [x] M1：Vault 访问核心（目录树 / 读写 / watchdog 监听 / 忽略规则 / 备份引擎）
- [x] M2：全文索引与检索（SQLite FTS5 + jieba，中文分词，增量更新）
- [x] M3：后端 API 层（vault / search / index / backup / agent 五组路由）
- [x] M4：前端浏览页面（Vue3 四视图 + 工程化构建）
- [x] M5：AI Agent（LLM 适配 / 工具集 / HITL 写前确认 / 写前备份）
- [ ] M6：测试补全、文档完善、v0.1.0 发布

---

## 0.1.0 - 待发布（首个 MVP）

### 新增
- 首次发布。功能基线见 [docs/01-功能说明.md](01-功能说明.md) 中 ✅ 标记的全部功能。

---

## 版本记录格式模板（开发者发布时复制使用）

```markdown
## [x.y.z] - YYYY-MM-DD

### 新增
- ...

### 修复
- ...

### 变更
- ...

### 移除
- ...

### 文档
- ...
```
